.map(style=mapStyle)
  each grid
    each this
      .grid-item(class=gridClass, data-x=x, data-y=y)
        .overlay
        
  each units
    +Unit

.container
  with round
    if isCurrentPlayer
      .btn.add-unit Add unit
      .btn.next-turn Next turn
